// One Agument, if statement

var age = 27;

function gabrielAge(age)


if (age == 27) {
  return "Correct, That is Gabriel's Age"}
  else
  return "Incorrect, That's not Gabriel's Age"
}

// Display output
console.log(gabrielage());

/* Expected output
Correct, That is Gabriel's Age